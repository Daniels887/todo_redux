import styled from 'styled-components'

export const ButtonStyled = styled.button`
  width: 100%;
  height: 40px;
  border: none;
  border-radius: 4px;
  background: rgba(0, 0, 255);
  color: white;
  font-weight: bold;
  cursor: pointer;
  transition: all 0.3s;
`
